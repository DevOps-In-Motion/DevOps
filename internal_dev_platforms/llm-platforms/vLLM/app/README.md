# Deprecated

This `app/` package stub is retired. Use:

**[`../ray_batch_inference/`](../ray_batch_inference/)** — Ray Data + vLLM batch inference (small-model local defaults; Databricks AIR `train.yaml` for scale).

See that directory’s README for install and `./scripts/run-local.sh`.
