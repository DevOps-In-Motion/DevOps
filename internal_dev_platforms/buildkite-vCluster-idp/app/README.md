# App directory overview

This is the directory that contains a vCluster based IDP for a branch based strategy that contains a helm chart. This assumes you have microservices with different repos and a centralized helm chart. 