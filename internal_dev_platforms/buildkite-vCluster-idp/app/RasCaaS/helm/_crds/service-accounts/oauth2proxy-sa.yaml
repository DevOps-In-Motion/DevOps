apiVersion: v1
kind: ServiceAccount
metadata:
  name: oauth2proxy-sa
  namespace: rascaas
  labels:
    app.kubernetes.io/name: oauth2proxy
    app.kubernetes.io/component: oauth2-proxy
  annotations:
    # QA IRSA — CSI mount of rascaas-secrets (SM). Same role as rascaas-sa.
    eks.amazonaws.com/role-arn: arn:aws:iam::YOUR_AWS_ACCOUNT_ID:role/platform-rascaas
