apiVersion: v1
kind: ServiceAccount
metadata:
  name: rascaas-sa
  namespace: rascaas
  labels:
    app.kubernetes.io/name: rascaas
    app.kubernetes.io/component: fastapi
  annotations:
    # QA IRSA — CSI mount of rascaas-secrets (SM). Role: iam/policy-platform-rascaas.json
    eks.amazonaws.com/role-arn: arn:aws:iam::650251729525:role/platform-rascaas
